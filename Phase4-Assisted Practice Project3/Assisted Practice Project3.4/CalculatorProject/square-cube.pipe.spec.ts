import { SquareCubePipe } from './square-cube.pipe';

describe('SquareCubePipe', () => {
  it('create an instance', () => {
    const pipe = new SquareCubePipe();
    expect(pipe).toBeTruthy();
  });
});
